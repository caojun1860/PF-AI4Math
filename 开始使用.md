# 开始使用

豆包模型已配置：doubao-seed-2-1-turbo-260628。
接口：普通火山方舟 API。凭据已配置，短文本与文件读取工具调用验收通过。

## 使用有预算保护的研究面板

1. 仅在第一次使用时，双击「配置豆包网关密钥.command」。若被编辑器打开，在 Finder 中右键，选择“打开方式 → 终端”。
2. 在终端提示处输入火山方舟 API Key 的实际密钥，按回车。输入时不会显示任何字符。密钥写入 macOS 钥匙串，不写进项目。
3. 双击「启动研究面板.command」。它会启动仅监听本机的预算网关，再进入 OpenCode。
4. 默认进入 `research-orchestrator` 总控 Agent。按 `Tab` 可直接切换 `math-deriver`、`symbolic-checker`、`literature-reader`、`lean-verifier` 或 `proof-reviewer`；总控也可用 `@` 委派。专业 Agent 的模式为 `all`，因此两种入口都不会回退总控。这些角色分别使用 `reasoning`、`fast`、`reader` 或 `reviewer` 模型别名；当前都映射到豆包，以后可分别改接 DeepSeek、Kimi 和 GPT。
5. 双击「查看预算.command」检查使用量；双击「调整预算.command」设置新的 token、人民币估算和请求数上限。

当前豆包专用总上限为 10,000,000 token、人民币 100 元估算和 10,000 次请求。高额度通过供应商前缀锁定到豆包；未来接入 GPT、Kimi 或 DeepSeek 时必须单独设置预算。网关在调用前保守预留输入和最大输出额度，任一维度不足便拒绝请求。正常结束后按供应商返回的实际 token 结算；拿不到 usage 或调用结果不确定时按预留上限结算，以免低估。

「启动直接模型面板.command」使用原有 OpenCode 凭据并绕过预算账本，只用于故障排查。不要用它执行长任务。

## 本地工具

「验证计算.command」执行已有符号检查，不调用模型。样例 PDF 位于 paper/build/environment-check.pdf。

## 已完成首个 Skill 试验

SymPy 技能已安装，新的 OpenCode 会话可按需加载；已打开的旧会话建议重启。完整对照见 reports/skill-comparison.md，修订后的证明与计算见 reports/reviewed/。

Lean 4 技能也已固定到项目。形式证明位于 formal/AI4MathFormal/Inequality.lean，验收见 reports/lean-formalization.md。

## MCP 与多 Agent

Zotero 和 Lean 已成为 OpenCode 可调用的本地 MCP。文献 Agent 只能读取 Zotero，Lean Agent 只能检查 `formal/`；子 Agent 均不能改文件或继续派生任务。双击「检查MCP与Agent.command」可以验证两个 MCP 和 Agent 权限，不调用模型。

第一张任务卡位于 `tasks/T001-agent-workflow-pilot.md`。打开研究面板后，复制任务卡末尾的建议启动语即可。总控在第一轮最多调用一个专业 Agent，得到结果后停止；继续下一轮由人决定。

当前已切换为低消耗模式：总控默认使用 `fast`，每轮只调用一个专业 Agent，汇总限制为 800 个汉字。需要长证明时再按 `Tab` 手动切换 `math-deriver` 或 `lean-verifier`。

如果任务是文献检索，可按 `Tab` 直接切到 `literature-reader`。它当前检索本地 Zotero；尚未接入互联网学术搜索，不能把本地库结果称为系统综述。

「启动研究面板.command」会先检查安全余量：8,000 token、0.05 元估算和 2 次请求。任一不足都会停止在模型启动之前；这不会影响「检查MCP与Agent.command」等纯本地工具。

双击「查看工作流状态.command」可以集中查看预算、模型角色、Agent、MCP 和 Git 检查点。该页面完全本地、只读，不消耗 token。

## 统一入口

以后可直接双击「AI4Math控制台.command」。菜单中的状态、MCP 检查、Lean 验证和预算查看均为本地操作；选择“打开 Agent 研究面板”时仍会经过预算安全线检查。

让外部模型读取 Zotero 检索结果会把选中的文献元数据或截断文本发送给当前模型供应商。运行文献 Agent 前，应只选择任务所需条目，并确认可以发送这些内容；本地检查脚本不会外发文献数据。
